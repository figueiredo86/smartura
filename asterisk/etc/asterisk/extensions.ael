context default {
	_X. => {
		Verbose(Tentativa de discagem - ${CALLERID(num)} para ${EXTEN} - contexto DEFAULT);
		Wait(2);
		Hangup();
	}
}

context receptivo {
	4000 => {
		AELSub(ura,${EXTEN});
	}
#include /etc/asterisk/macros_ura.ael
}
